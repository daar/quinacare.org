---
import OptimizedImage from "./OptimizedImage.astro";
import {
  getLangFromUrl,
  useTranslations,
  getLocalizedPath,
  type Lang,
} from "../i18n";

interface Props {
  lang?: Lang;
}

const lang = Astro.props.lang ?? getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section class="bg-qcGray py-24 border-y border-gray-100">
  <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-2 gap-16 items-center">
    <div class="rounded-2xl overflow-hidden shadow-xl">
      <OptimizedImage
        src="/media/2024/11/HPV-project.png"
        alt={t("focus.title")}
        class="w-full h-[500px] object-cover"
        widths={[400, 600]}
        sizes="(max-width: 768px) 100vw, 50vw"
      />
    </div>
    <div>
      <span
        class="text-qcRed font-bold text-xs uppercase tracking-widest mb-4 block"
        >{t("focus.label")}</span
      >
      <h2 class="text-4xl font-bold mb-6">{t("focus.title")}</h2>
      <p class="text-gray-600 text-lg mb-8 leading-relaxed font-georgia">
        {t("focus.description")}
      </p>
      <a
        href={getLocalizedPath("/projects/hpv-screening", lang)}
        class="inline-block bg-qcRed text-white px-10 py-4 font-bold uppercase text-xs tracking-widest hover:bg-qcBlack transition rounded-md"
      >
        {t("focus.cta")}
      </a>
    </div>
  </div>
</section>
