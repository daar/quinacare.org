dist
.astro
temp

# Generated output, not hand-edited source.
#
# reports/  — donations dashboards written by scripts/donations-report.py;
#             the HTML is emitted by the script, so reformatting it only
#             produces churn on the next run.
# missesRedirects.mjs — written by scripts/resolve-404s.mjs, one redirect
#             per line on purpose so a diff shows routes that changed
#             rather than rewrapped lines.
reports
src/data/missesRedirects.mjs
